# Docker Configuration
# =====================================================
# Path to Docker socket for container management
# The backend uses this to communicate with Docker daemon

# macOS (Docker Desktop)
DOCKER_HOST=unix:///Users/<your-username>/.docker/run/docker.sock
